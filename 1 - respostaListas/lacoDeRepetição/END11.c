#include <stdio.h>
#include <stdlib.h>
int main(int argc, char *argv[])
{
    int num = atoi(argv[1]);

    if (num >= 4000)
    {
        puts("numero maior igual a 4000 --> invalido");
    }
    else if (num > 0)
    {
        
    }

    return 0;
}