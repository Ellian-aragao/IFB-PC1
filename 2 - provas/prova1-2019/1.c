#include <stdio.h>
int main()
{
    int x;
    scanf("%d",&x);
    if (x > 2 && x < 1e6)
    {
        printf("%d =",x);
        while ( x != 1)
        {
            for (int i = 2; i <= x; i += 2)
            {
                
            }
        }
    }

    return 0;
}