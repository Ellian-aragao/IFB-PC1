#include <stdio.h>
#include <stdlib.h>
int main()
{
    int x;
    scanf("%d",&x);
    long int *primos = malloc(1 * sizeof(long int));
    primos[0] = 2;
    while (1)
    {
        
    }

    return 0;
}